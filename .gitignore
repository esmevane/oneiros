/target
/tmp
node_modules/
# Component package build outputs are local-only.
packages/components/dist/
# Token CSS and dashboard SPA dist are generated artifacts that get committed
# (regenerated by `cargo xtask` and consumed by the engine).
.worktrees/
.claude/worktrees/
.claude/worktrees/**/target
*.local.*

exports

# MCP config (contains auth tokens)
.mcp.json
