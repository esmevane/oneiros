---
description: Reconcile a followed bookmark
argument-hint: <name>
---

Run `oneiros bookmark collect $ARGUMENTS` to pull events from every source the named bookmark follows. Reconciliation is set-based via chronicle Merkle diff — only the events your project doesn't already have are transferred.
