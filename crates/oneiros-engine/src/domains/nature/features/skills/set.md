---
description: Define a kind of relationship between things
argument-hint: "<name> [--description <text>] [--prompt <text>]"
---

Run `oneiros nature set $ARGUMENTS` to create or update a nature. Natures categorize connections — they describe the structural relationship between two linked entities.

Use `--description` for a human-readable explanation and `--prompt` for the agent-facing guidance text.
