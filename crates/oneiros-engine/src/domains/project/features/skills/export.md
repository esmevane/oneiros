---
description: Export all brain events to a portable archive
argument-hint: "--target <path>"
---

Run `oneiros project export --target <path>` to export all events from the current brain to a file. The export is a complete snapshot of the event log, suitable for backup or transfer to another host.
