`oneiros project follow <uri> --name <name>`

Create a repository peer by following a project share URI. After following, you can submit bookmarks to and collect bookmarks from that peer.

```bash
oneiros project follow oneiros://alice-host/link:<ticket> --name alice
```
