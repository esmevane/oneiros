---
description: Import brain events from an archive
argument-hint: <file>
---

Run `oneiros project import <file>` to import events from a previously exported brain archive. Events are deduplicated — re-importing the same archive is safe.
