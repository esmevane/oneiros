---
description: Define a quality of connection between thoughts
argument-hint: "<name> [--description <text>] [--prompt <text>]"
---

Run `oneiros sensation set $ARGUMENTS` to create or update a sensation. Sensations categorize experiences — they describe the quality of connection between cognitive records.

Use `--description` for a human-readable explanation and `--prompt` for the agent-facing guidance text.
