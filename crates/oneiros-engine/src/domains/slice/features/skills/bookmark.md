---
description: Create a bookmark from a slice for sharing and transport
argument-hint: "<slice-name> --as <bookmark-name>"
---

Run `oneiros slice bookmark $ARGUMENTS` to create a bookmark from a slice. The bookmark is a transportable snapshot suitable for sharing with `oneiros bookmark share`.
