---
description: Remove a stored artifact
argument-hint: <key>
---

Run `oneiros storage remove $ARGUMENTS` to delete a storage entry by key. If other keys reference the same content blob, the blob is preserved.
