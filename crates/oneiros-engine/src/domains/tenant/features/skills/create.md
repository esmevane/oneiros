---
description: Create a new tenant on this host
argument-hint: <name>
---

Run `oneiros tenant create $ARGUMENTS` to create a new tenant. Tenants are the top-level organizational units — each tenant owns a set of actors who can access brains.
