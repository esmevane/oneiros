---
description: Issue a brain access ticket to an actor
argument-hint: "--actor-id <id> --brain-name <name>"
---

Run `oneiros ticket issue $ARGUMENTS` to issue a ticket granting an actor access to a brain. Tickets are the authentication tokens that authorize brain-scoped operations.
