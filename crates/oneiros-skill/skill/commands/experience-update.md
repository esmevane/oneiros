---
description: Update an experience's description or sensation
argument-hint: "<id> [--description <text>] [--sensation <name>]"
---

Run `oneiros experience update $ARGUMENTS` to update an experience. Requires the experience ID (or 8+ char prefix) and at least one of `--description` or `--sensation`.

Both flags can be provided together to update both fields in one command.
