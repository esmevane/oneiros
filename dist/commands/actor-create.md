---
description: Create a new actor under a tenant
argument-hint: "<name> --tenant-id <id>"
---

Run `oneiros actor create $ARGUMENTS` to create a new actor associated with the given tenant. Actors are the authenticated identities that access brains — they hold tickets that grant brain access.
