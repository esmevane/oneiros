---
description: Create a connection between two linked entities
argument-hint: "<nature> <from-link> <to-link>"
---

Run `oneiros connection create $ARGUMENTS` to create a connection edge between two links. The nature must already exist (see `oneiros nature set`). Links are base64url-encoded content addresses.
