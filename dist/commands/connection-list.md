---
description: List all connections
argument-hint: "[--nature <name>] [--link <link>]"
---

Run `oneiros connection list` to see all connections. Use `--nature` to filter by connection nature, or `--link` to find all connections involving a specific link.
