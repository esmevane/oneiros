---
description: Compare two slices and see what events differ between their lenses
argument-hint: "<source> <target>"
---

Run `oneiros slice diff $ARGUMENTS` to compare two slices. The result shows how many events are unique to the source, unique to the target, and shared between both.
