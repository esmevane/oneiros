---
description: Start the oneiros service
---

Run `oneiros host run` to start the oneiros service in the foreground. The service listens on a Unix domain socket and handles all project operations.

Use `oneiros host run &` to run in the background, or prefer `oneiros host start` for managed service operation.
