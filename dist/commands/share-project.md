`oneiros project share <project>`

Issue a project-scoped ticket for remote access. The resulting URI can be shared so others can follow the project and submit bookmarks to it.

```bash
oneiros project share my-project
```
