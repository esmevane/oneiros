---
description: Remove a lens-filtered slice by name
argument-hint: "<name>"
---

Run `oneiros slice delete $ARGUMENTS` to remove a slice. Only the slice metadata is deleted — the underlying events in the project log are unaffected.
