---
description: Install oneiros as a managed user service
---

Run `oneiros service install` to install oneiros as a managed user service (launchd on macOS). The service will start automatically on login.
